from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A beginner project to predict if a person dies of heart failure',
    author='Samrakshini',
    license='',
)
